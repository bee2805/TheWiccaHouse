import { AuthOnlyDirective } from './auth-only.directive';

describe('AuthOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
